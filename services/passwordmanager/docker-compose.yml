services:

# VAULTWARDEN
  vaultwarden:
    image: vaultwarden/server:latest
    container_name: vaultwarden
    restart: unless-stopped
    environment:
     - ADMIN_TOKEN=${VAULTWARDEN_ADMIN_TOKEN}
     - WEBSOCKET_ENABLED=true
     - SIGNUPS_ALLOWED=false
     - DOMAIN=https://${VAULTWARDEN_DOMAIN}
    networks:
      - public
    volumes:
     - ${DATADIR}/vaultwarden:/data:rw
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.vaultwarden.rule=Host(`${VAULTWARDEN_DOMAIN}`)"
      - "traefik.http.routers.vaultwarden.entrypoints=websecure"
      - "traefik.http.routers.vaultwarden.tls=true"
      - "traefik.http.routers.vaultwarden.tls.certresolver=letencrypt"
      - "traefik.http.services.vaultwarden.loadbalancer.server.port=${VAULTWARDENPORT}"

# NETWORKS
networks:
  public:
    external: true